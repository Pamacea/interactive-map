import { auth as nextAuth } from "@/app/api/auth/[...nextauth]/route";

/**
 * Helper function to get the current session server-side
 * Compatible with NextAuth v5 (auth.js)
 */
export async function auth() {
  return await nextAuth();
}
